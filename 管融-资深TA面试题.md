# 管融 — 资深TA技术面试题

> 候选人：管融 | 29岁 | 7年经验 | 求职：技术美术（资深）  
> 简历亮点：网易主美/高级TA、二次元NPR渲染、URP/HDRP管线、Shader编写、移动端性能优化  
> 面试时长：30分钟 | 共7题（选问5-6题）

---

## 评分参考

| 能力维度 | 对应题目 | 强信号（过） | 弱信号（不过） |
|---------|---------|------------|--------------|
| **渲染架构设计** | Q1 | 主动设计分层体系，提出美术不碰Shader的封装方案 | 只会写Shader，没有工具链/材质体系设计意识 |
| **PBR物理正确性** | Q2 | 能写出能量守恒公式，一眼看出代码缺少(1-F)扣除项 | 只会调Metallic/Roughness参数，不涉及物理原理 |
| **管线深度理解** | Q3 | 准确说出RenderPassEvent枚举、Pass顺序逻辑、SRP Batcher断批条件 | 只用过ShaderGraph，不了解管线内部执行流 |
| **性能诊断体系** | Q4 | 有系统化的CPU/GPU诊断方法论，知道降分辨率测试法、平台Profiler | 凭经验调参，无系统性诊断流程 |
| **PCG程序化生成** | Q5 | 能设计可美术导的程序化流程，理解分层覆盖与实例化策略 | 只会操作Houdini节点，无管线设计与Unity集成意识 |
| **工程决策力** | Q6 | 用实测数据驱动决策，给降级替代方案，沟通方式成熟 | 笼统说"性能不够"，无量化、无替代方案 |
| **团队与流程** | Q7 | 设计全链路检查节点，强调"左移"思维和自动化校验 | 只关注引擎内优化，无DCC端/流程前置意识 |

> **面试节奏建议：** Q1→Q2先考察渲染根基（NPR架构+PBR原理），Q3→Q4考察管线与性能深度，Q5考察PCG程序化能力，Q6考察工程决策力，Q7放在最后考察管理维度。根据候选人简历侧重选问5-6题，时间紧时Q7可简化为口头描述。

---

## Q1. NPR渲染体系架构设计（6分钟）

**题目：** 你要为新项目从零搭建一套二次元NPR渲染管线。从TA架构层面，你会怎么设计这套体系的分层？Shader、材质、工具链各负责什么？如何让美术在不需要理解Shader的前提下高效产出？

**追问（深入点）：** 你提到法线修正对NPR很重要。二次元角色面部渲染中，如果直接用模型原始法线做 `N·L` 光照计算，面部阴影会非常脏乱（鼻子、嘴部出现碎片阴影）。你会怎么解决这个问题？请说明你的法线修正方案，以及如何在DCC端和Shader端配合实现。

**考察点：** 是否真正做过二次元面部阴影渲染，而非只了解概念。法线修正是NPR面部渲染的核心痛点，能区分"调参型"和"原理型"TA。

### 参考答案

**分层架构：**

| 层 | 职责 | 示例 |
|---|------|------|
| **Shader层** | 核心着色逻辑，所有NPR特征（色阶、描边、面部SDF、头发高光等）的数学实现 | `CharacterNPR.shader` 内含多个Pass/Feature |
| **Material层** | 对Shader参数的语义化封装，通过自定义Inspector面板暴露美术可理解的参数 | 如"阴影色阶"、"描边宽度"而非`_RampThreshold` |
| **工具链层** | 自动化美术流程：法线修正工具、SDF面部贴图烘焙、材质批量替换、LOD生成 | Editor脚本 + DCC端脚本 |

**关键设计原则：**

- **Shader变体管理**：用Shader Feature而非多Shader文件，控制变体数量在移动端预算内（如<128）
- **美术不碰Shader**：通过自定义Material Inspector + 预设Material模板，美术只需调面板参数
- **验证闭环**：OverDraw/DrawCall/变体数等指标在Editor面板实时显示，美术改完立刻看到性能影响
- **DCC端规范**：法线平滑组、UV接缝、顶点色用途（如描边宽度控制）在建模阶段就标准化

**强信号：** 候选人能主动提出"美术不需要理解Shader"的工程化思路，并描述具体的Inspector封装或预设模板方案。

**追问参考答案：**

面部原始法线问题：鼻子、嘴唇等区域法线方向混乱，`N·L` 计算出的阴影边界锯齿状、碎片化，角色转动时阴影剧烈抖动。

**方案一：法线平滑（最常用）**

- **DCC端**：将面部法线统一朝向正前方（或Z轴），抹平鼻子、嘴部等凹凸区域的法线差异。可用3ds Max的"法线"修改器或脚本，将面部法线reset到一个平坦方向
- **Shader端**：直接用修正后的法线做 `NdotL`，阴影边界变成一条干净的曲线，不再有碎片
- **缺点**：面部立体感会丢失（因为法线被抹平了），需要靠贴图（法线贴图/Bake贴图）补回细节

**方案二：SDF面部阴影图（进阶，业界主流方案）**

- **DCC端**：为面部烘焙一张SDF（Signed Distance Field）贴图，记录面部阴影边界的有向距离场
- **Shader端**：不再用 `N·L` 算面部阴影，而是用光照方向（`lightDir` 的XY分量）旋转采样SDF贴图，直接得到干净的阴影边界
- **优点**：阴影边界完全可控，美术可以精确调整阴影形状；角色转动时阴影平滑过渡，无抖动
- **缺点**：每个角色面部需要单独烘焙SDF贴图，制作成本较高

**方案三：Lock-to-Camera 光照方向**

- **Shader端**：将面部光照方向锁定到摄像机方向（或固定方向），角色转动时光照方向跟随旋转，保持面部阴影不抖动
- **通常与方案一/方案二组合使用**，而非单独使用

**DCC与Shader配合要点：**
- DCC端负责"输出正确数据"：平滑法线或SDF贴图
- Shader端负责"用正确方式消费数据"：用修正法线做 `NdotL`，或用SDF采样替代 `NdotL`
- 两者必须配套——如果DCC端做了法线平滑但Shader端仍用原始法线逻辑，修正无效

**强信号：** 能说出SDF方案并解释其原理；能区分"抹平法线"和"SDF替代N·L"两种思路的本质区别；提到Lock-to-Camera作为辅助手段；强调DCC端与Shader端必须配套。

---

## Q2. PBR物理正确性与能量守恒（6分钟）

**题目：** 你简历提到熟悉PBR流程。请回答：

1. BRDF的能量守恒具体指什么？写出公式直觉。
2. 以下哪段代码违反了能量守恒？为什么？

```hlsl
// 方案A
float3 specular = D * G * F / (4 * NdotL * NdotV);
float3 diffuse  = baseColor / PI;
float3 result   = (diffuse + specular) * NdotL * lightColor;

// 方案B
float3 specular = D * G * F / (4 * NdotL * NdotV);
float3 diffuse  = baseColor / PI * (1 - F);
float3 result   = (diffuse + specular) * NdotL * lightColor;
```

3. 如果美术反馈"金属看起来太暗"或"非金属高光太强"，你会分别检查什么？

**考察点：** 是否真正理解PBR的数学原理，而非只是"调过参数"。能量守恒是PBR的分水岭——会用和懂原理差距很大。

### 参考答案

**1. 能量守恒含义：**

入射光的能量 = 反射（镜面）+ 漫反射（漫射）+ 吸收。即：

$$f_r = f_{specular} + f_{diffuse}$$

且两者之和不能超过入射光能量。直觉上：**表面越光滑，镜面反射越集中越亮，但漫反射必须相应减弱——总反射能量恒定。** 反之越粗糙，镜面反射越分散（单位面积看起来更暗），漫反射占比增大。

**2. 方案A违反能量守恒：**

方案A中 `diffuse = baseColor / PI`，**没有减去镜面反射所占的能量比例**。这意味着同一束光，一部分被镜面反射走了，但漫反射仍然按"全量"计算，总反射能量 > 入射能量，违反守恒。

方案B中 `diffuse = baseColor / PI * (1 - F)`，用 Fresnel 项扣除了镜面反射的能量占比，漫反射只拿到"剩余能量"，物理正确。

> **进阶追问（如果时间允许）：** 严格来说方案B也不完全精确——精确的能量守恒需要对BRDF在半球面积分，而 `(1 - F)` 只是一个近似。但在实时渲染中这个近似足够用。真正精确的做法是预计算 `kd = 1 - kS`（对镜面BRDF积分），Unity HDRP内部就是这么做的。

**3. 排查方向：**

| 美术反馈 | 检查方向 |
|---------|---------|
| **金属太暗** | ① 检查Metallic值是否为1（金属的diffuse应为0，全部能量走specular）；② 检查BaseColor——金属的BaseColor代表F0（反射率），有色金属F0通常为0.5~0.95，如果美术填了很低的值（如0.2），金属必然暗；③ 检查环境反射（IBL）是否正确，金属高度依赖环境反射获得亮度 |
| **非金属高光太强** | ① 检查Metallic值是否过高——非金属的F0应在0.02~0.05之间，如果Metallic偏高会让F0错误飙升；② 检查Smoothness/粗糙度是否过高（高光滑 → 高光集中 → 看起来刺眼）；③ 检查是否缺了能量守恒——diffuse没有扣除specular能量，总反射超标 |

**强信号：**

- 能写出 `f_r = f_specular + f_diffuse` 并解释两者此消彼长关系
- 能一眼看出方案A缺了 `(1 - F)` 并解释原因
- 金属排查提到"F0"和"IBL依赖"，非金属排查提到"Metallic误用导致F0飙升"
- 如果候选人主动说"方案B也是近似，精确做法是预积分kd"，说明深度理解

**弱信号：**

- 只说"PBR就是用Metallic/Roughness工作流"，不涉及能量守恒公式
- 看代码无法判断哪个违反守恒
- 排查思路是"调参数试试"而非从物理原理反推

---

## Q3. URP管线定制深度 — 从Render Feature到管线重构（6分钟）

**题目：** 你在URP下做过自定义Render Feature。请回答：

1. Render Feature的执行时机在管线中的哪个位置？`RenderPassEvent`有哪些关键插入点？
2. 如果二次元渲染需要"屏幕空间描边 + 角色特殊光照 + 后处理色调映射替换"，这三个自定义Pass的执行顺序应该怎么排？为什么？
3. SRP Batcher在什么条件下会断批？自定义Render Feature会不会影响SRP Batcher？

**考察点：** 是否真正理解URP管线内部执行流程，而非仅调API。

### 参考答案

**1. 执行时机：**

Render Feature通过`RenderPassEvent`枚举指定插入点，关键位置：

- `BeforeRenderingOpaques` — 不透明物体渲染前
- `AfterRenderingOpaques` — 不透明物体渲染后（描边Pass常用，此时深度已写入）
- `BeforeRenderingTransparents` / `AfterRenderingTransparents` — 透明物体前后
- `BeforeRenderingPostProcessing` / `AfterRenderingPostProcessing` — 后处理前后

**2. 三Pass顺序：**

1. **角色特殊光照Pass** → `AfterRenderingOpaques`（基于已有深度/法线，在主光照后叠加自定义光照）
2. **屏幕空间描边Pass** → `AfterRenderingOpaques`（在角色全部渲染后做边缘检测，但不能晚于透明物体，否则UI/粒子边缘也会被描边）
3. **色调映射替换Pass** → `BeforeRenderingPostProcessing`（替换或自定义URP的Tonemapping，确保最终输出颜色符合NPR风格）

**3. SRP Batcher断批条件：**

- SRP Batcher要求同一Shader的Material使用相同的CBUFFER布局。如果Material之间的属性差异导致CBUFFER不匹配，会断批
- Shader中使用了不被SRP Batcher兼容的关键字（如`UnityInstancing`的某些宏使用不当）
- **自定义Render Feature本身不直接影响SRP Batcher**，因为它是在Pass层面操作，不影响Material的CBuffer匹配。但如果Render Feature中修改了渲染状态（如混合模式、深度测试）导致DrawCall无法合批，则间接影响

**强信号：** 能准确说出`RenderPassEvent`的具体枚举值并解释原因；能区分"Render Feature影响渲染顺序"和"影响SRP Batcher合批"是两件事。

---

## Q4. 移动端性能瓶颈诊断体系（6分钟）

**题目：** 场景在高端手机上60FPS，低端机上掉到25FPS。你作为资深TA，会怎么系统性地定位瓶颈？GPU瓶颈和CPU瓶颈的判断方法是什么？如果是GPU Bound，你怎么进一步区分是Vertex还是Fragment瓶颈？

**考察点：** 是否有系统化的性能诊断方法论，而非"凭经验调参数"。

### 参考答案

**Step 1: CPU vs GPU判断**

- **Unity Profiler**：看CPU和GPU各自的帧时间。如果CPU <16ms但GPU >16ms → GPU Bound
- **帧率锁定测试**：将分辨率降到50%，如果帧率明显提升 → GPU Bound（Fragment瓶颈）；如果不变 → CPU Bound或Vertex Bound
- **Android: GPU Profiler（Mali Graphics Debugger / Adreno Profiler / Snapdragon Profiler）**：直接看GPU各阶段耗时

**Step 2: GPU Vertex vs Fragment区分**

- **降分辨率测试**：分辨率降一半帧率显著提升 → Fragment Bound（片元太多）；不变 → Vertex Bound
- **降顶点测试**：开启激进的LOD/减面，帧率提升 → Vertex Bound
- **Vertex Bound常见原因**：蒙皮动画计算量（每帧骨骼计算）、顶点Shader复杂度（多次矩阵变换）、DrawCall过多导致Vertex Fetch瓶颈
- **Fragment Bound常见原因**：OverDraw过高、半透明叠加过多、Fragment Shader计算过重（如多次纹理采样、复杂光照计算）

**Step 3: 针对性优化**

- **Vertex优化**：GPU Skinning、LOD、顶点精简、合并网格
- **Fragment优化**：减少OverDraw（遮挡剔除、前向渲染避免多光源）、简化Shader（减少采样次数、降低精度）、使用PrePass（低精度G-Buffer）
- **带宽优化**：移动端TBR架构下，避免频繁的RenderTexture切换、合理使用Tile Memory

**强信号：** 能说出"降分辨率50%测试法"这种实际诊断手段；能解释TBR架构与带宽瓶颈的关系；提到具体平台的GPU Profiler工具。

---

## Q5. Houdini PCG程序化生成与Unity集成（6分钟）

**题目：** 你做过Houdini PCG程序化生成。假设要程序化生成一个大尺度城市场景（建筑、道路、植被），结果需要导入Unity。请回答：

1. 你怎么设计Houdini端的程序化流程，让美术能控制生成结果（如建筑密度、风格切换），而不需要打开Houdini？
2. 生成结果导入Unity时，几千栋建筑如何处理DrawCall？你会用什么策略？
3. 如果美术对PCG生成的某几栋建筑做了手动调整，重新跑PCG后这些手动修改怎么保留？

**考察点：** 是否真正设计过程序化管线，而非只在Houdini里连节点。三个子问题分别考察：美术可导性、性能集成、迭代工作流——这三个维度能区分"会用Houdini"和"能用Houdini做生产管线"。

### 参考答案

**1. 美术可导的程序化流程设计：**

- **HDA（Houdini Digital Asset）封装**：将PCG逻辑打包为HDA，暴露参数面板（建筑密度、楼层范围、风格选择、种子值等），美术在Houdini或UE/Unity（通过Houdini Engine插件）中直接调参数
- **参数分层**：区分全局参数（城市规模、路网密度）和局部参数（单栋建筑高度、材质风格），美术可全局控制也可逐区域微调
- **风格预设**：预设几套风格模板（现代/科幻/废土），美术一键切换，而非逐参数调
- **随机种子控制**：暴露Seed参数，美术可锁定满意的布局，再微调局部

**2. DrawCall处理策略：**

- **GPU Instancing**：相同Mesh+Material的建筑用GPU Instancing合批，几千栋建筑可能只需几十个DrawCall
- **Mesh合并策略**：将同区域、同风格的建筑合并为一个大Mesh（Static Batching），减少DrawCall但增加内存
- **LOD分级**：PCG生成时自动生成多级LOD，远处建筑用低精度Mesh
- **材质图集（Texture Atlas）**：将不同建筑的贴图合并为Atlas，统一Material，减少材质切换导致的DrawCall断裂
- **遮挡剔除**：城市场景中大量建筑互相遮挡，配合Occlusion Culling减少不可见建筑的提交

**3. 手动修改保留——分层覆盖工作流：**

这是PCG生产管线最核心的问题，也是区分"玩具级PCG"和"生产级PCG"的分水岭。

- **分层架构**：
  - **Base Layer（PCG生成层）**：程序化生成的原始结果，每次重新跑PCG会完全重建
  - **Override Layer（手动修改层）**：美术手动调整的建筑、删除的建筑、新增的道具，存在独立的层/数据中
  - **合并规则**：Override Layer优先级高于Base Layer，重新生成Base Layer时不触碰Override Layer
- **实现方式**：
  - **按ID/坐标标记**：每栋PCG建筑分配唯一ID或锚定世界坐标，手动修改记录到Override数据中，重新生成时按ID匹配恢复
  - **Scatter点+手动增删点**：PCG输出的是Scatter点云（建筑放置点），美术可手动删除/移动/新增点，这些修改作为Override点云保存，重新跑PCG时Merge两者
  - **Unity侧Prefab覆盖**：PCG导入时每个建筑对应一个Prefab实例，美术对Prefab实例的修改（缩放、旋转、材质替换）通过Prefab Override保留，重新导入PCG数据时只更新Base Prefab，不覆盖实例Override

**强信号：**
- 能主动提出"分层覆盖"（Base + Override）的架构思路
- DrawCall方案能说出GPU Instancing + Atlas + LOD的组合拳
- 美术可导方案提到HDA封装和参数分层
- 提到"按ID/坐标匹配恢复手动修改"这种具体的工程实现思路

**弱信号：**
- 只说"用Houdini生成然后导出FBX"，无管线设计意识
- DrawCall只说"合并Mesh"，不提GPU Instancing
- 对手动修改保留问题回答"重新生成会覆盖，没办法"或"让美术别改"

---

## Q6. 渲染质量与性能的Trade-off决策（6分钟）

**题目：** 项目美术要求角色Shader必须支持"次表面散射（SSS）+ 多层描边 + 风格化高光"，但移动端GPU预算只有3ms/帧给角色渲染。你怎么做技术评估和决策？你会怎么跟美术沟通？

**考察点：** 资深TA的核心能力——在质量与性能之间做工程判断，并能推动落地。

### 参考答案

**技术评估框架：**

| 特效 | 移动端真实成本 | 降级方案 |
|------|-------------|---------|
| SSS | 高（需Prepass + Blur + 组合，多次采样+多次RT切换） | 用Ramp贴图模拟皮肤透光感，或用预烘焙厚度贴图 + 单次计算 |
| 多层描边 | 中高（每个描边Pass额外一遍Mesh渲染，DrawCall翻倍） | 合并为单Pass描边，用顶点色控制描边宽度差异 |
| 风格化高光 | 低（Fragment Shader内计算，额外1-2次采样） | 基本可保留，优化采样次数即可 |

**决策逻辑：**

1. **量化每个特效的GPU耗时**：在目标机型上实测，不能凭感觉
2. **3ms预算分配**：高光0.5ms + 描边1ms + 主体渲染1.5ms = 3ms，SSS必须降级或砍掉
3. **分层方案**：高端机开SSS（简化版），中端机用Ramp模拟，低端机纯色阶

**跟美术沟通方式（考察软技能）：**

- **先给可对比的视觉效果**：在目标机上跑出"有SSS vs 降级方案"的对比截图，让美术自己看差异是否可接受
- **用量化数据说话**：不要说"性能不够"，而是说"SSS在骁龙765上额外耗时2.1ms，会导致帧率从60掉到42"
- **给替代方案而非直接拒绝**：提供Ramp模拟的视觉替代，而非单纯说"做不到"
- **锁定底线**：明确告知移动端预算硬约束，由美术决定在预算内怎么取舍

**强信号：** 主动提出"目标机型实测"而非纸面估算；能给出具体的降级替代方案；沟通方式是"给数据和对比"而非"技术说不行"。

---

## Q7. 团队建设与流程标准化（6分钟）

**题目：** 你在网易管理过20人美术团队。如果一个新项目从0启动，你会怎么搭建TA与美术的协作流程？具体来说：

1. 美术资产从DCC产出 → 导入引擎 → 上线，你会设立哪些技术检查节点？
2. 如何防止美术在后期集中暴雷（性能/规范问题堆积到最后）？

**考察点：** 资深TA的流程设计能力，是否能把技术规范前置到生产链路中。

### 参考答案

**1. 检查节点设计：**

| 阶段 | 检查内容 | 工具/手段 |
|------|---------|----------|
| **DCC产出阶段** | 法线平滑组、UV接缝、顶点数预算、命名规范 | DCC端脚本自动检查（MaxScript/Python），不合规直接标红提示 |
| **导入阶段** | 贴图尺寸/格式、LOD完整性、材质引用正确性 | Unity AssetPostprocessor 自动校验，不合规拒绝导入并报错 |
| **预览阶段** | DrawCall数、OverDraw、Shader变体数、内存占用 | Editor面板实时显示 + CI定时扫描全场景 |
| **上线前** | 目标机型真机测试、内存峰值、发热/掉帧 | 自动化真机测试报告 |

**2. 防止后期暴雷的核心思路——"左移"（Shift-Left）：**

- **规范前置**：在美术开工前就产出《美术技术规范文档》，明确每个资产类型的约束（顶点数上限、贴图分辨率、DrawCall预算）
- **工具前置**：DCC端就做检查，不合规的资产在制作阶段就被拦截，而不是导入引擎后才发现
- **持续监控**：每周跑一次全场景性能报告，DrawCall/内存趋势可视化，发现趋势性恶化立即介入
- **验收门槛**：每个美术资产提交时自动检查，不通过Build无法打包，强制合规

**强信号：**

- 能说出"AssetPostprocessor自动校验"这种具体技术手段
- 强调"规范前置"和"持续监控"而非"后期优化"
- 提到CI/自动化检查流程，说明有工程化思维
